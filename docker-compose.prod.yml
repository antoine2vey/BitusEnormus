version: "3"
services:
  bot:
    build: .
    volumes:
      - .:/usr/app
      - /usr/app/node_modules/
    command: npm start
    depends_on:
      - mongodb
    links:
      - mongodb
    env_file:
      - .env
    ports:
      - 5858:5858

  mongodb:
    image: mongo
    ports:
      - 27018:27017